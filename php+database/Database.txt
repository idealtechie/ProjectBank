cd\
cd xampp
cd mysql
cd bin
mysql.exe -u root
create database sample;
use sample;
create table student(sname varchar(20),regno INT,m1 INT,m2 INT);

------------------------------------------
After inserting data in html file.
use select * from student in cmd to view the inserted value.